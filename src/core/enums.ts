export enum DocumentType {
	WorkCard = 'workCards',
	Customer = 'customers',
	Employee = 'employees'
}

export enum MainRoutes {
	Home = "/",
	Customers = '/customers',
	Employees = '/employees',
	AddWorkCard = '/workcard/add'
}
